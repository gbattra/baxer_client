import { profile } from './profile-default'
import { tracks } from './tracks-default'
import { playlists } from './playlists-default'
import { badges } from './badges-default'
import { routes } from './routes-default'


export const AllDefaults = {
  badges: badges,
  playlists: playlists,
  profile: profile,
  routes: routes,
  tracks: tracks
}
